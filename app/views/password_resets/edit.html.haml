= error_messages_for :user
- form_tag(password_reset_url, :method => :put) do
	new password:
	= password_field_tag 'user[password]'
	%br
	confirm new password:
	= password_field_tag 'user[password_confirmation]'
	%br
	= submit_tag "save!"